{
  "name": "chisel-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "migrate:all": "bun src/db/migrate-all.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/hono": "latest",
    "@modelcontextprotocol/server": "latest",
    "hono": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
